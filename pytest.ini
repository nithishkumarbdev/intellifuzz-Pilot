[pytest]
asyncio_mode = auto
; Our own TestCase model (app.runner.models.TestCase) is not a pytest
; test class, but pytest's default collection heuristics would try to
; treat it as one just because of its name. We only use function-based
; tests, so disable class-based collection entirely.
python_classes =
